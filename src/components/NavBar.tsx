import React from "react";

const NavBar: React.FC = () => {
  return (
    <nav className="bg-gray-800 py-2 flex justify-center">
      <span className="text-lg font-semibold text-white">Navbar</span>
    </nav>
  );
};

export default NavBar;
